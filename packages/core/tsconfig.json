{
  "extends": "../../tsconfig.base.json", 
  "compilerOptions": {
    "outDir": "./dist",
    "declaration": true,
    "module": "NodeNext",
    "rootDir": "./src",
    "composite": true // 이미 설정되어 있음
  },
  "include": ["src"],
  "exclude": ["node_modules", "dist"],
  "references": [
    {
      "path": "../types" // types 패키지 참조 추가
    }
  ]
}